# Create all the models

Create all the models

## Usage

``` r
create_models(TreMs)
```

## Arguments

- TreMs:

  A TreMs dataframe

## Value

A dataframe containing all the models

## Examples

``` r
if (FALSE) { # \dontrun{
mod_df <- create_models(TreMs)
} # }
```
