# Load the data and prepare it for analysis

Load the data and prepare it for analysis

## Usage

``` r
load_data(MasterThesisData)
```

## Arguments

- MasterThesisData:

  A dataframe probably from MasterThesisData2024.csv

## Value

A dataframe of tree data

## Examples

``` r
if (FALSE) { # \dontrun{
  TreMs <- load_data(MasterThesisData2024)
} # }
```
